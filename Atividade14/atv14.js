document.getElementById("up").addEventListener('change', () => {
    entrada.value= entrada.value.toUpperCase();
});

document.getElementById("low").addEventListener('change', () => {
    entrada.value= entrada.value.toLowerCase();  
});